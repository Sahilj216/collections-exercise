Question 1
Write Java code to define a List. Insert 5 floating point numbers in the list, and using an iterator, find the sum of the numbers in the list.

Question 2

Given the following class:
```java
class Employee {
    Double age;
    Double salary;
    String firstName;
    String lastName;
}
```

Design the class in such a way that the default sorting works on firstName and lastName.
Also, write a program to sort Employee objects based on salary using a Comparator.

 Question 3
Design a data structure SpecialStack that supports all the stack operations like push(), pop(), isEmpty(), isFull() and an additional operation getMin() which should return the minimum element from the SpecialStack in O(1) time.

Question 4
Create a class Employee with attributes name, age, and designation.
Use instances of this class as keys in a Map and store their salary as value.
Ensure proper equals() and hashCode() methods are implemented so that keys work correctly.

